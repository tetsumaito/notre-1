# Astro (ブログ生成ツール) のソース一式はデプロイリポジトリに含めない
# Cloudflare Pages はリポジトリをそのまま配信するため、ソースが公開されてしまう。
# ソースはローカル/Driveで管理し、ビルド済みの /blog だけをコミットする。
_blog-astro/

# その他
node_modules/
.DS_Store

# Claude Code ローカル設定（コミットしない）
.claude/
