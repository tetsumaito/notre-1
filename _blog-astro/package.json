{
  "name": "notre-blog",
  "type": "module",
  "private": true,
  "version": "1.0.0",
  "description": "notre.co.jp /blog をMarkdownから生成するAstro。出力は ../blog（静的デプロイのまま）。",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && npm run sync",
    "sync": "rsync -a --delete dist/ ../blog/",
    "preview": "astro preview",
    "serve": "echo 'リポジトリ全体をローカル配信(ヘッダー/CSS込み): cd .. && python3 -m http.server 8080 → http://localhost:8080/blog/'"
  },
  "dependencies": {
    "astro": "^5.6.0",
    "remark-cjk-friendly": "^2.0.1"
  }
}
